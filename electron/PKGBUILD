# Maintainer: Anti-UAV Project
pkgname=anti-uav-control-center
pkgver=3.0.0
pkgrel=1
pkgdesc="Anti-UAV Control Center Desktop App"
arch=('x86_64')
url="https://github.com/SafSaf0999/UAV"
license=('MIT')
depends=('docker' 'electron28')
source=("${pkgname}-${pkgver}.AppImage")
sha256sums=('SKIP')

package() {
    install -Dm755 "${srcdir}/${pkgname}-${pkgver}.AppImage" \
        "${pkgdir}/usr/bin/${pkgname}"
}
