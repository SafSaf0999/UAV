[tool.pytest.ini_options]
addopts = "--hypothesis-seed=0 -v"
testpaths = ["edge/tests", "main/aggregation/tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]

[tool.hypothesis]
# Default profile — overridden by conftest.py
max_examples = 50
